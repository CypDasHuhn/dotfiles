---
name: shell
description: Use when running shell commands, writing shell scripts, or choosing shell syntax. The configured shell is nushell — use it unless the task explicitly requires another shell.
---

# Shell

When calling the shell, use nushell, since it's already our shell.
